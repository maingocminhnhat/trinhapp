// import { User } from '../../models';

const usersResolves  = {
  // Query: {

  // },
  Mutation: {
    registerUser: async () => {
      // See if an old user exists with email atempting to register

      // Throw error if that user exists

      // Encrypt password

      // Build out mongoose model

      // Create our JWT (attach to our User model
      
      // Save our user in MongoDB
    }
  }
}

export {
  usersResolves
}
